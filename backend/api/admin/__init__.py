"""Admin API package — sub-routers grouped by domain.

Imported via the legacy ``admin_routes`` module which re-exports the
combined router for back-compat with anything that does
``from .admin_routes import register_admin_routes``.
"""
