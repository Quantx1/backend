# ============================================================================
# QUANT X - DEV / TEST / LINT / DOCS DEPENDENCIES
# ----------------------------------------------------------------------------
# Deliberately NOT installed in the API image. Install locally or in CI with:
#   pip install -r requirements.txt -r requirements-dev.txt
# ============================================================================

# ── Testing ────────────────────────────────────────────────────────────────
pytest>=7.4.4
pytest-asyncio>=0.23.3
pytest-cov>=4.1.0
pytest-mock>=3.12.0
faker>=22.0.0
fakeredis>=2.20.0

# ── Code quality ───────────────────────────────────────────────────────────
black>=24.1.1
flake8>=7.0.0
mypy>=1.8.0
isort>=5.13.2
pre-commit>=3.6.0
pylint>=3.0.3

# ── Security scanning (used by the CI `security` job) ──────────────────────
bandit>=1.7.0
safety>=3.0.0

# ── Documentation ──────────────────────────────────────────────────────────
mkdocs>=1.5.3
mkdocs-material>=9.5.3
